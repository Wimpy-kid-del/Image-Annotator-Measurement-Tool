This project is a Windows desktop application developed using Python and OpenCV.
It allows users to load images, draw annotations (rectangle, line, circle), add text,
measure distances between two points, and save annotated images.

- Python
- OpenCV (cv2)
- Tkinter / PyQt5
- NumPy

1. Install required libraries
2. Run the main Python file
